@model List<Proveedor>
@{
    ViewData["Title"] = "Index";
}

<body>
    <main class="main-wrap">
        <section class="content-main">
            <div class="content-header">
                <div>
                    <h2 class="content-title card-title">Lista Proveedores</h2>
                </div>
                <div>
                    <a href="@Url.Action("Create", "Proveedor")" class="btn btn-primary btn-sm rounded">Crear nuevo cliente</a>
                </div>
            </div>
            <div class="card mb-4">
                <div class="card-body">
                    <article class="itemlist">
                        @foreach (var proveedor in Model)
                        {
                            <div class="row align-items-center">
                                <div class="categoria-fila" style="display:flex;gap:25px;">
                                    <h6 class="mb-0">@proveedor.idproveedor</h6>
                                    <h6 class="mb-0" style="width:150px;">@proveedor.nombre</h6>
                                    <h6 class="mb-0" style="width:150px;">@proveedor.direccion</h6>
                                    <h6 class="mb-0" style="width:150px;">@proveedor.telefono</h6>
                                    <h6 class="mb-0" style="width:150px;">@proveedor.email</h6>
                                    <a href="@Url.Action("Edit", "Proveedor", new { id = proveedor.idproveedor })" class="btn btn-sm font-sm rounded btn-brand"> <i class="material-icons md-edit"></i> Editar </a>
                                    <form action="@Url.Action("Delete", "Proveedor", new { id = proveedor.idproveedor })" method="post" style="display:inline;" onsubmit="return confirm('¿Está seguro de que desea eliminar este proveedor?');">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm font-sm btn-light rounded">
                                            <i class="material-icons md-delete_forever"></i> Eliminar
                                        </button>
                                    </form>
                                </div>
                            </div>
                            <br />
                        }
                    </article>
                </div>
            </div>
        </section>
    </main>
</body>
